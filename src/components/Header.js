import React from "react";
import logo from "../images/logo.svg";

const Header = () => {
  return (
    <div className="logo">
      <img src={logo} alt="logo" />
    </div>
  );
};

export default Header;
